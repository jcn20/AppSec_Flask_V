sudo: true
dist: bionic
language: python
        - "3.6"

addons: 
        apt:
                packages:
                        - "python3"
                        - "python3-pip"
before_install: 
        - sudo apt-get update
install: 
        - pip3 install -r requirements.txt
script: 
        - tox
