sudo: true
dist: trusty
language: python
        - "3.6"

addons: 
        apt:
                packages:
                        - "python3"
                        - "python3-pip"
before_install: 
        - sudo apt-get update
install: 
        - sudo pip3 install -r requirements.txt
script: 
        - tox
